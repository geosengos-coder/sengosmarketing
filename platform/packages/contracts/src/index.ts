/**
 * @operatoros/contracts — the shared Zod schemas and inferred types that define
 * the shape of every API input. One source of truth for validation across the
 * web app, the voice-gateway, and (later) the public API.
 */
export * from "./common";
export * from "./organization";
export * from "./ai-employee";
